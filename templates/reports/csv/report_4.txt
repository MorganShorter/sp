Product code;Product name;Minimum;QTY in stock;Sum of QTY out;Last In QTY;Last In Date
{% for o in object_list %}{{ o.code }};{{ o.name }};{{ o.minimum_stock }};{{ o.current_stock }};{{ o.stock_out }};{{ o.last_order.quantity }};{{ o.last_order.order.order_date|date }}
{% endfor %}